const useCustomHook = (number) => {
	console.log('Initial value: ', number);

	// useEffect(
	// 	() => {
	// 		console.log('Props.number = ', number);
	// 	},
	// 	[ number ]
	// );
};

export default useCustomHook;
